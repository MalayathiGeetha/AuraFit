server:
  port: 8080

spring:

  datasource:
    url: jdbc:mysql://localhost:3306/fitness_user_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: geetha123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
      # use create / create-drop / validate / update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/

#  jackson:
#    serialization:
#      indent_output: true
#
#logging:
#  level:
#    org.hibernate.SQL: DEBUG
#    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
